name: main.yaml
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World from GitHub Actions!"

########## Code snippet from buoi11 part2 for reference ##########
# name: server_prod
# on:
#   push:
#     branches:
#       - main
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to production server
#         uses: appleboy/ssh-action@v1.0.3
#         with:
#           host: ${{ secrets.VPS_HOST }}
#           username: ${{ vars.VPS_USER }}
#           key: ${{ secrets.VPS_KEY }}
#           script: |
#             cd /root/docker
#             git pull
#             docker-compose down
#             docker-compose build
#             docker-compose up -d
#             docker images -f "dangling=true" -q | xargs docker rmi

########## Code snippet from buoi11 part1 for reference ##########
# name: buoi11.yaml
# on:
#   push:
#     branches:
#     - main
#   # workflow_dispatch:

# jobs:
#   job_1:
#     runs-on: ubuntu-latest
#     outputs:
#       data: "${{steps.step_id.outputs.data_in}}"
#     steps:
#       - id: step_id
#         run: |
#           echo "data_in=1" >> $GITHUB_OUTPUT

#   job_2:
#     runs-on: ubuntu-latest
#     needs: [job_1]
#     outputs:
#       data: "${{steps.step_id.outputs.data_in}}"
#     steps:
#       - id: step_id
#         run: |
#           echo "data_in=2" >> $GITHUB_OUTPUT

#   job_3:
#     runs-on: ubuntu-latest
#     needs: [job_1,job_2]
#     steps:
#       - name: Gửi mail
#         if: $(( ${{needs.job_1.outputs.data}} ${{needs.job_2.outputs.data}} )) == 3
#         uses: dawidd6/action-send-mail@v3
#         with:
#           server_address: ${{vars.MAIL_SERVER_ENDPOINT_URL}}
#           server_port: ${{vars.MAIL_SERVER_ENDPOINT_PORT}}
#           username: ${{vars.MAIL_SERVER_USER_NAME}}
#           password: ${{vars.MAIL_SERVER_APPLICATION_PASSWORD}}
#           subject: "Đây là tiêu đề gửi từ githubv action"
#           body: "API trả về 200, kèm file response.json"
#           to: thanhvantruong.vn@gmail.com
#           from: thanhvantruong.vn@gmail.com

########## Code snippet from buoi10 part 1 for reference ##########
# name: buoi10.yaml
# on: [push]
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - run: echo "Hello World buoi10 from GitHub Actions!"
